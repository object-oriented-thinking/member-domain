package our.member.auth.fixture;

import java.util.UUID;

public class AuthenticationFixture {
    public static final UUID APPLICANT_MEMBER_ID = UUID.randomUUID();
    public static final UUID NOT_INVALID_EMAIL_MEMBER_ID = UUID.randomUUID();
}
